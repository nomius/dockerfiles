FROM scratch
ARG ROOTFS
ARG IMAGE_VERSION
ARG IMAGE_SUBVERSION
LABEL DISTRIBUTION=Kwort
LABEL VERSION=$IMAGE_VERSION
LABEL SUBVERSION=$IMAGE_SUBVERSION
ADD $ROOTFS ./
ENV SHELL=/bin/bash
CMD ["bash", "-l"]

